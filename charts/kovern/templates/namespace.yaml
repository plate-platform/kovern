apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Release.Namespace }}
  labels:
    {{- include "kovern.labels" . | nindent 4 }}
    kovern.io/exclude: "true"  # Exclude kovern-system from webhook enforcement
